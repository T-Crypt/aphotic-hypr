# Theme Spec

Placeholder — populated in Phase B (Identity) alongside the wallust/matugen
color engine work. A theme directory will need to declare: a reference
wallpaper, the engine + backend/colorspace/scheme pin, and optional
per-app config overrides.
