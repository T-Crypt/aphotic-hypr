import QtQuick

// Hand-written stand-in for Caelestia's native BeatTracker plugin type.
// Not used by any Phase 1 bar component — only exists so Audio.qml's
// existing instantiation resolves.
QtObject {
}
