(() => { const root=document.documentElement, themes=['abyss','signal','violet','amber']; const saved=localStorage.getItem('aphotic-accent')||themes[0]; root.dataset.accent=saved; document.querySelector('.accent-toggle')?.addEventListener('click',()=>{const next=themes[(themes.indexOf(root.dataset.accent)+1)%themes.length];root.dataset.accent=next;localStorage.setItem('aphotic-accent',next)}); const menu=document.querySelector('.menu-toggle'), nav=document.querySelector('#site-nav'); menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',open);nav.dataset.open=open}); document.querySelectorAll('pre').forEach(pre=>{const b=document.createElement('button');b.type='button';b.className='copy';b.setAttribute('aria-label','Copy code');b.textContent='copy';b.onclick=()=>navigator.clipboard.writeText(pre.querySelector('code')?.innerText||'').then(()=>{b.textContent='copied';setTimeout(()=>b.textContent='copy',1200)});pre.prepend(b)}) })();
